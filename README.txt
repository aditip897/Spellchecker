Algorithm that traverses through given document and compares words to a provided dictionary. Determines if words in the document are spelled incorrectly, in comparision to the dictionary.

Built using C. 
